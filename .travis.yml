dist: trusty
sudo: required

before_install:
  - sudo apt-get install libffi-dev
  - curl -kL https://raw.github.com/saghul/pythonz/master/pythonz-install | bash
  - source $HOME/.pythonz/etc/bashrc
  - pythonz install 2.7.13

install:
  - $(pythonz locate 2.7.13) --version

script:
  - env
